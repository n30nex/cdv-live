version: "3.9"

services:
  meshviz:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./config.txt:/app/config.txt:ro
      - ./data:/app/data
    restart: unless-stopped
